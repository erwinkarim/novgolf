.jumbotron.jumbotron-fluid{ :style => "background: url(/images/golf_course_#{rand(1..4)}.jpg); height:70vh; background-size:cover"}
  .container
.container
  .row
    .col-xs-12.col-md-8
      %h1 #{ @club.name }
      %div
        = simple_format(@club.description)
    .col-xs-12.col-md-4
      .card
        .card-block
          %h4 Availablity
        = react_component "GolfSchedule", {crsfToken:form_authenticity_token,
          club: @result[:club],
          path: { reserveTarget: reserve_golf_club_user_reservations_path(params[:id]),
            golfClub:golf_club_path(params[:id]) },
          reserveTarget: reserve_golf_club_user_reservations_path(params[:id]),
          prices:@result[:prices]  ,
          teeTimes: @result[:tee_times],
          flight: @result[:flight].merge( { selectedPax:"2"})}
.container
  .row
    .col-xs-12
      %h2 Location (demo for now)
#map{ :style => "height:70vh;"}
:javascript
  var map;
  var initMap = function(){
      map = new google.maps.Map(document.getElementById('map'), {
        center: {lat: -34.397, lng: 150.644},
        zoom: 8
      });
  };
  initMap();

.card
  .card-block
    %p.card-text Hello #{@resource.name || @resource.email} !
    %p.card-text Someone has requested a link to change your password. You can do this through the link below.
    %a.btn.btn-primary{ :href => edit_password_url(@resource, reset_password_token: @token)} Change Password
    %p.card-text If you didn't request this, please ignore this email.
    %p Your password won't change until you access the link above and create a new one.
%p Thank you,
%p JomGolf

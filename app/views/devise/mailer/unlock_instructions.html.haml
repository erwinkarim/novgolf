.card
  .card-block
    %p.card-text Hello #{ @resource.name || @resource.email}
    %p.card-text Your account has been locked due to an excessive number of unsuccessful sign in attempts.
    %p.card-text Click the link below to unlock your account:</p>
    -#unlockable module not implemented/included yet
    -#%a.btn.btn-primary{:href=> unlock_url(@resource, unlock_token: @token)}
